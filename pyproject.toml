[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "Reusable maze generator module"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["mazegen", "mazegen.solver"]
